<!DOCTYPE html>
<html lang="en">
<head>
  <title>{{ title | default("Virgil's Potting Shed") }}</title>

  <meta name="description" content="{{ description | default('Musings, lore, and ramblings from Virgil’s potting shed.') }}" />

  <meta property="og:title" content="{{ title | default('Virgil’s Potting Shed') }}" />
  <meta property="og:description" content="{{ description | default('Musings, lore, and ramblings from Virgil’s potting shed.') }}" />
  {% if image %}
    <meta property="og:image" content="{{ image }}" />
  {% endif %}
  <meta property="og:type" content="website" />
  <meta charset="UTF-8" />
  <link rel="stylesheet" href="/css/styles.css" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
</head>
<body>
  {% include "header.njk" %}

  <div class="container">
    <aside class="left-column">
      <!-- Keith Gadgets or other widgets here -->
    </aside>

    <main class="center-column">
      {{ content | safe }}

      {% if page.tags and "/posts/" in page.inputPath %}
        <p>Tags:
          {% for tag in page.tags %}
            <a href="/tags/{{ tag | slug }}/" class="tag-link">{{ tag }}</a>{% if not loop.last %}, {% endif %}
          {% endfor %}
        </p>

        <p>Categories:
          {% for category in page.categories %}
            <a href="/categories/{{ category | slug }}/" class="category-link">{{ category }}</a>{% if not loop.last %}, {% endif %}
          {% endfor %}
        </p>
      {% endif %}
    </main>

    <aside class="right-column">
      <!-- More widgets, calendar, etc. -->
    </aside>
  </div>

  {% include "footer.njk" %}
  {% include "scripts.njk" %}
</body>
</html>
