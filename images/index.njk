---
title: Welcome to Virgil’s Potting Shed
layout: base.njk
---

<section class="welcome-box">
  <h1>Welcome to Virgil’s Potting Shed</h1>
  <p>Step inside the rustic world of slugs, brassicas, and curious ramblings. Here you'll find musings, odd tales, and the occasional wisdom gleaned from compost and cosmic slugs.</p>
</section>

<section class="post-grid">
  {% for post in collections.posts | slice(0,6) %}
    <article class="post-card">
      {% if post.data.image %}
        <img src="{{ post.data.image }}" alt="Thumbnail for {{ post.data.title }}">
      {% endif %}
      <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
      <p>{{ post.templateContent | truncate(120, true, '…') }}</p>
      <div class="post-buttons">
        <a href="{{ post.url }}">Read More</a>
      </div>
    </article>
  {% else %}
    <p>No posts found yet — keep an eye out for Virgil’s next rambling!</p>
  {% endfor %}
</section>

